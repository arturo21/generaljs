/*!
 * @pluginjs/classes v0.7.16 (https://pluginjs.com)
 * Copyright 2019 Creation Studio Limited
 * Released under the GPL-3.0 License.
 */
'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var utils = require('@pluginjs/utils');
var is = require('@pluginjs/is');

var hasClass = utils.curry((classes, element) => {
  var has = true;
  classes.split(' ').forEach(className => {
    has = has && element.classList.contains(className);
  });
  return has;
});
var indexOfClass = utils.curry((className, element) => element.classList.item(className));
var addClass = utils.curryWith(function () {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }

  var classes = args.slice(0, -1);
  var element = args.slice(-1)[0];

  if (is.isElement(element)) {
    if (classes.length === 1) {
      classes = classes[0].split(/\s+/g).filter(v => v !== '');
    }

    element.classList.add(...classes);
  }

  return element;
}, is.isElement);
var removeClass = utils.curryWith(function () {
  for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
    args[_key2] = arguments[_key2];
  }

  var classes = args.slice(0, -1);
  var element = args.slice(-1)[0];

  if (is.isElement(element)) {
    if (classes.length === 1) {
      classes = classes[0].split(/\s+/g).filter(v => v !== '');
    }

    element.classList.remove(...classes);
  }

  return element;
}, is.isElement);
var toggleClass = utils.curry((className, element) => {
  element.classList.toggle(className);
  return element;
});

exports.addClass = addClass;
exports.hasClass = hasClass;
exports.indexOfClass = indexOfClass;
exports.removeClass = removeClass;
exports.toggleClass = toggleClass;
